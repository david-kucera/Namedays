<Window x:Class="Uniza.Namedays.EditorGuiApp.EditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Uniza.Namedays.EditorGuiApp"
        mc:Ignorable="d"
        Title="EditWindow" Height="150" Width="300">
    <Grid>
        <Grid VerticalAlignment="Top" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="Name: "/>
            <TextBox Name="TextBox" Grid.Column="1"/>
        </Grid>
        <Grid VerticalAlignment="Top" Margin="16,50,10,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="Date: "/>
            <DatePicker Name="DatePicker" Grid.Column="1"/>
        </Grid>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Right">
            <Button Click="Close" Content="OK" Padding="30,5" Margin="5"/>
            <Button Click="Close" Content="Cancel" Padding="20,5" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
